/* En los hash tables, la 'key' pasa por una 'Hash function', esta function transforma
la key que le pasamos en una dirección de memoria. Siempre que le pasemos la misma key,
se generará la misma dirección de memoria, por esta razón los hash tables son rápidos
para acceder a los valores */

